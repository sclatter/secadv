{{self-focusing-heading title="Engineering Work"}}
    <ul>
      {{#each model}}
      <li>
          <dl>
            <dt>{{project}}</dt>
            <dd>{{organization}}</dd>
            <dd {{action "focusSection"}}>{{#link-to 'engineering.details' itemId}}details <span class="hidden-text">Read more details about {{project}}.</span>{{/link-to}}</dd>
          </dl>
      </li>
    {{/each}}
    </ul>
{{outlet}}