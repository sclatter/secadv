<h2>Design Work</h2>
    <ul>
      {{#each model}}
      <li>
          <dl>
            <dt>{{project}}</dt>
            <dd>{{organization}}</dd>
            <dd {{action "focusSection" param=itemId}}>{{#link-to 'design.details' itemId}}details{{/link-to}}</dd>
          </dl>
      </li>
    {{/each}}
    </ul>
    {{outlet}}
